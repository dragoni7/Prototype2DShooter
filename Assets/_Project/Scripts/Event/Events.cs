namespace dragoni7
{
    /// <summary>
    /// Event Ids
    /// </summary>
    public enum Events : int
    {
        OnEntityDamaged,
        OnEntityMove,
        OnEntityAttack,
        OnEnemySpawned,
        OnPlayerSpawned,
        OnEntityDie
    }
}
